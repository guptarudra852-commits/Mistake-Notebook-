import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rudra.academicmistakenotebook',
  appName: 'Academic Mistake Notebook',
  webDir: 'dist',
};

export default config;
